mixin video(info)
  div 
    h4
      a(href=`/videos/${info.id}`)=info.title
    ul 
      li #{info.rating}/5
      li #{info.comments} coments.
      li Posted #{info.creatAt}.
      li #{info.views} views.
